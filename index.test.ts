import { FrontChannel } from "./index";

describe("front-channel-typescript-sdk", () => {
    it("initialize client", async () => {
        const frontchannel = new FrontChannel({
        });
    });
});
